name: AI PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  ai-review:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout PR code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          pip install google-generativeai PyGithub

      - name: Save AI script
        run: |
          cat > ai_review.py << 'SCRIPT_END'
          import os
          import sys
          from github import Github, Auth
          import google.generativeai as genai

          print("🔍 Running AI Review...")

          # Configure APIs
          genai.configure(api_key=os.getenv("GEMINI_API_KEY"))
          
          # Fix 1: Use new Auth method for PyGithub
          auth = Auth.Token(os.getenv("GITHUB_TOKEN"))
          g = Github(auth=auth)

          # Get PR details
          repo = g.get_repo(os.getenv("REPO_NAME"))
          pr = repo.get_pull(int(os.getenv("PR_NUMBER")))

          # Collect changed code
          changed_code = ""
          file_count = 0
          total_additions = 0
          total_deletions = 0

          for f in pr.get_files():
              file_count += 1
              total_additions += f.additions
              total_deletions += f.deletions
              
              patch = f.patch or ""
              if patch:
                  changed_code += f"### File: {f.filename}\n```diff\n{patch}\n```\n\n"

          # Check if there are changes to review
          if not changed_code:
              print("No code changes to review")
              sys.exit(0)

          # Limit the diff size to avoid token limits
          if len(changed_code) > 30000:
              changed_code = changed_code[:30000] + "\n\n... (diff truncated due to size)"

          prompt = f"""
          You are an expert code reviewer. Review this PR diff and provide feedback.

          Focus on:
          - 🐛 Bugs and potential issues
          - 🔒 Security concerns
          - ⚡ Performance improvements
          - 🎨 Code quality and best practices

          PR Statistics:
          - Files changed: {file_count}
          - Lines added: {total_additions}
          - Lines deleted: {total_deletions}

          PR Diff:
          {changed_code}
          """

          try:
              # Fix 2: Use correct model name - either gemini-pro or gemini-1.5-pro
              model = genai.GenerativeModel("gemini-pro")
              # Alternative: model = genai.GenerativeModel("gemini-1.5-pro")
              
              response = model.generate_content(prompt)
              
              # Format the comment
              comment = f"""### 🤖 AI Code Review

          **PR Summary:** {file_count} file(s) changed (+{total_additions} / -{total_deletions} lines)

          ---

          {response.text}

          ---

          *This is an automated review. Please verify suggestions before implementing.*
          """
              
              pr.create_issue_comment(comment)
              print("✅ AI Review posted successfully!")
              
          except Exception as e:
              print(f"❌ Error during review: {str(e)}")
              # Post a simple comment if AI review fails
              fallback_comment = f"""### 🤖 AI Code Review

          Unable to generate detailed review at this time.
          Error: {str(e)}

          **PR Summary:** {file_count} file(s) changed (+{total_additions} / -{total_deletions} lines)

          Please ensure:
          - Code follows project conventions
          - Changes are well-tested
          - No security vulnerabilities introduced
          """
              pr.create_issue_comment(fallback_comment)
              sys.exit(1)
          SCRIPT_END

      - name: Run AI Review Script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO_NAME: ${{ github.repository }}
        run: python ai_review.py